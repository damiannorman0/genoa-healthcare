import React from 'react';

const AppFooter = (props) => {
	return (
		<footer>
			<div>privacy</div>
			<div>contact us</div>
		</footer>
	);
};

AppFooter.propTypes = {
};

AppFooter.defaultProps = {
};

export default (AppFooter);
